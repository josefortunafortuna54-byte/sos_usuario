import 'package:flutter_test/flutter_test.dart';
import 'package:sos_usuario/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Apenas verifica que o app não crasha
  });
}
